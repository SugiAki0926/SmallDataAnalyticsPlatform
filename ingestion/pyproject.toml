[project]
name = "source-ingestion"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.44.2",
    "psycopg[binary]>=3.3.6",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
